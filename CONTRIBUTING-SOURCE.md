# 원본 소스 파일 요청 (빌드 전 소스)

## 배경

현재 이 저장소(`hw5511/ai-artwork`)에는 **빌드 결과물만** 포함되어 있습니다.

- `index.html` — Vite/React로 빌드된 약 870KB의 minify된 단일 번들
- `downloads/`, `lectures/`, `images/` — 다운로드용 워크플로우 JSON 및 이미지 자료
- `logo.svg`, `.nojekyll` — GitHub Pages 배포용 파일

강의 본문 텍스트와 구조(`session1~4`, `step1~N`, `tocData`, 라우트 키 등)는
전부 `index.html`의 압축된 자바스크립트 번들 안에 문자열로 박혀 있습니다.

## 문제

강의 **내용을 안전하게 수정**하려면 빌드 전 원본 소스가 필요합니다.
minify된 번들을 직접 고치는 것은 다음 이유로 권장되지 않습니다.

- 동일 문자열 중복 / 변수명 충돌로 인한 깨짐 위험이 큼
- 강의 구조 변경, 스텝 추가, UI/기능 수정은 사실상 불가능

## 필요한 파일 (빌드 전 원본 소스)

아래 구조의 원본 프로젝트를 업로드해 주세요.

```
package.json          # 의존성 및 빌드 스크립트
vite.config.*         # 빌드 설정 (Vite 추정)
index.html            # 빌드 전 템플릿 (현재의 압축본이 아닌 원본)
src/                  # 컴포넌트 + 강의 본문/구조 데이터 (.jsx/.tsx, tocData 등)
public/               # 정적 파일 (선택)
```

`package.json`, `src/`, 빌드 설정이 핵심입니다.

## 진행 방식 (택 1)

### 방법 A — 같은 저장소에 소스도 함께 두기
- 원본 소스를 이 저장소의 하위 폴더(예: `app/` 또는 `source/`)에 추가
- `src` 수정 → 빌드 → `index.html` 갱신 → 배포까지 한 저장소에서 처리
- 장점: 관리 단순 / 단점: 배포 저장소에 소스가 섞임

### 방법 B — 소스 전용 저장소 분리 (권장)
- 원본 소스 전용 저장소를 만들고, 빌드 결과물만 이 저장소로 배포
- 일반적인 프론트엔드 + GitHub Pages 운영 방식
- 해당 소스 저장소를 작업 세션에 연결

## 다음 단계

원본 소스가 준비되면 위 방법 중 하나를 선택해 알려 주세요.
이후 강의 내용 수정 → 빌드 → 배포 작업을 진행하겠습니다.
